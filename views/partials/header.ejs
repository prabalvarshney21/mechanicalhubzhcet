<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= typeof title !== 'undefined' ? title : 'MechanicalHubZhcet' %></title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .navbar-glass {
      background: rgba(10,10,15,0.98);
      border-bottom: 1.5px solid #172554;
      backdrop-filter: blur(8px) saturate(160%);
      box-shadow: 0 6px 30px 0 rgba(10,15,38,0.30);
    }
  </style>
</head>
<body class="bg-gradient-to-br from-black to-blue-900 text-white font-sans min-h-screen">
  <nav class="navbar-glass sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-8 py-3 flex justify-between items-center">
      <a href="/" class="flex items-center gap-4">
        <img src="/logo.png"
             alt="Logo"
             onerror="this.onerror=null;this.src='/favicon.ico';"
             class="h-14 w-14 rounded-2xl shadow-lg border-2 border-blue-700 bg-white object-contain p-2"/>
      </a>
      <ul class="flex space-x-8 font-semibold text-lg items-center">
        <li><a href="/notes" class="hover:text-blue-400 transition-all duration-150">Notes</a></li>
        <li><a href="/pyqs" class="hover:text-blue-400 transition-all duration-150">PYQs</a></li>
        <li><a href="/forum" class="hover:text-blue-400 transition-all duration-150">Forum</a></li>
        <li><a href="/contact" class="hover:text-blue-400 transition-all">Contact Us</a></li>
        <% if (!session.admin) { %>
          <li><a href="/login" class="hover:text-blue-400 transition-all">Login</a></li>
        <% } else { %>
          <li><a href="/admin" class="hover:text-blue-400 transition-all">Admin</a></li>
          <li><a href="/logout" class="hover:text-blue-400 transition-all">Logout</a></li>
        <% } %>
      </ul>
    </div>
  </nav>
  <main class="min-h-screen py-8 px-4 max-w-6xl mx-auto">
