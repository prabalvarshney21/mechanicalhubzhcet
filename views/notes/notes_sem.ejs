<%- include('partials/header') %>
<div class="main-container">
  <h2><%= year %> Year – <%= sem.toUpperCase() %> Subjects</h2>
  <div class="btn-grid">
    <% subjects[sem].forEach(subject => { %>
      <a href="/notes/<%= year %>/<%= sem %>/<%= encodeURIComponent(subject) %>" class="btn"><%= subject %></a>
    <% }) %>
  </div>
</div>
<%- include('partials/footer') %>